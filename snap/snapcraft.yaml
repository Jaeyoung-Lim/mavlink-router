name: mavlink-router # you probably want to 'snapcraft register <name>'
version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: snap for mavlink router  # 79 char long summary
description: |
  This is the snap for the MAVLink Router(https://github.com/Jaeyoung-Lim/mavlink-router.git).
  The MAVLink Router is used to route mavlink packets between end points. 


grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
